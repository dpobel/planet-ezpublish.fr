<div class="block">
    <h1><a href="{{ path( blogs ) }}">{{ blogs.contentInfo.name }}</a></h1>
    <ul>
    {% for site in sites %}
        <li>{{ ez_render_field( site, "url" ) }}</li>
    {% endfor %}
    </ul>
</div>
